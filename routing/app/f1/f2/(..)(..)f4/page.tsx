export default function InterceptedF4() {
  return <h1>(..)(..) Intercepted F4</h1>;
}

// every (..) in the name tells next.js how many levels above is your desired component
// (.) tells next.js that the intercepted component is on the same level
